# raw HTTP upstream：mihomo 原生 HTTP 代理节点。
# 不需要认证时可以删除 username/password。
- name: server-http
  type: raw
  config:
    type: http
    server: http.example.com
    port: 8080
    username: up-user
    password: up-pass
