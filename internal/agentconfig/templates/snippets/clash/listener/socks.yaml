# Clash socks listener：供本机或 xrelay 连接，名称会被 ref 引用。
- name: local
  listen: 127.0.0.1
  port: {{ .SocksListenerPort }}
  # users 不写表示跟随全局 authentication；users: [] 表示该 listener 不认证。
  # users:
  #   - username: local-user
  #     password: local-pass
